{
  "name": "wiki_data",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "format": "bunx prettier . --write",
    "build": "bun run format && bun index.ts"
  },
  "type": "module",
  "dependencies": {
    "@types/ejs": "^3.1.5",
    "ejs": "^3.1.10"
  }
}
